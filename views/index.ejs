<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viu API - Video Streaming</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>🎬 Viu API</h1>
            <p>Simple Video Streaming API Endpoint</p>
        </header>

        <main class="content">
            <!-- Overview -->
            <section class="section">
                <h2>📚 Overview</h2>
                <p>A fast and simple API to access video data and streaming metadata from Viu. Built with Node.js, Express, and EJS.</p>
                <div class="badge-group">
                    <span class="badge">Node.js</span>
                    <span class="badge">Express</span>
                    <span class="badge">Fast Response</span>
                    <a href="/test" style="display: inline-block;"><span class="badge" style="background: #28a745; cursor: pointer;">🧪 Test API</span></a>
                </div>
            </section>

            <!-- API Endpoints -->
            <section class="section">
                <h2>🔌 API Endpoints</h2>
                
                <div class="endpoint">
                    <h3>1. Home Data</h3>
                    <code class="endpoint-url">GET /api/home</code>
                    <p>Get featured content from Viu home</p>
                    <details>
                        <summary>View Example</summary>
                        <pre><code>curl https://your-domain.com/api/home</code></pre>
                    </details>
                </div>

                <div class="endpoint">
                    <h3>2. Search Video</h3>
                    <code class="endpoint-url">GET /api/search</code>
                    <p>Search videos by query</p>
                    <table class="params-table">
                        <thead>
                            <tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr>
                        </thead>
                        <tbody>
                            <tr><td>q</td><td>string</td><td>Yes</td><td>Search query</td></tr>
                            <tr><td>page</td><td>number</td><td>No</td><td>Page (default: 1)</td></tr>
                            <tr><td>count</td><td>number</td><td>No</td><td>Items per page (default: 18)</td></tr>
                        </tbody>
                    </table>
                </div>

                <div class="endpoint">
                    <h3>3. Product Detail</h3>
                    <code class="endpoint-url">GET /api/detail/:id</code>
                    <p>Get detailed information and episode list</p>
                    <table class="params-table">
                        <thead>
                            <tr><th>Parameter</th><th>Type</th><th>Description</th></tr>
                        </thead>
                        <tbody>
                            <tr><td>id</td><td>string</td><td>Product ID (in URL)</td></tr>
                        </tbody>
                    </table>
                </div>

                <div class="endpoint">
                    <h3>4. Stream Metadata</h3>
                    <code class="endpoint-url">GET /api/stream/:ccsProductId</code>
                    <p>Get streaming metadata for a specific product</p>
                    <table class="params-table">
                        <thead>
                            <tr><th>Parameter</th><th>Type</th><th>Description</th></tr>
                        </thead>
                        <tbody>
                            <tr><td>ccsProductId</td><td>string</td><td>CCS Product ID (in URL)</td></tr>
                        </tbody>
                    </table>
                </div>
            </section>

            <!-- Response Format -->
            <section class="section">
                <h2>📋 Response Format</h2>
                <p>All responses follow this format:</p>
                <pre><code>{
  "success": true,
  "data": { /* Response data */ }
}

// On error:
{
  "success": false,
  "error": "Error message"
}</code></pre>
            </section>
        </main>

        <footer class="footer">
            <p>Made with ❤️ | Viu API v1.0 | Fast & Simple Video Streaming</p>
        </footer>
    </div>
</body>
</html>
